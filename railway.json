{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt && python scripts/ingest_ncert.py"
  },
  "deploy": {
    "startCommand": "gunicorn \"app:create_app()\" --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
